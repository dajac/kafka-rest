// Default runtime options
export let options = {
	vus: 100,
    duration: "30s",
    summaryTrendStats: ["avg", "min", "med", "max", "p(90)", "p(95)", "p(99)"]
};

// Base URL that we prepend to all URLs we use
export let urlbase = "http://localhost:9391";
